# Manim GUI Builder

GUI-based editor for building Manim animations:

- Canvas-based object editing
- Property panels with drag-adjust inputs
- Timeline animation editor
- Frontend preview (approximate)
- Backend code generation + Manim render

This repo follows `AGENTS.md` as the source of truth for architecture and MVP scope.

