[project]
name = "manim-gui-backend"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
  "jsonschema>=4.22"
]

[project.optional-dependencies]
render = [
  "manim>=0.18"
]

[tool.uvicorn]
factory = false
